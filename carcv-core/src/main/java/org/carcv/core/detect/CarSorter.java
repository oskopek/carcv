/**
 * 
 */
package org.carcv.core.detect;

import java.util.List;

import org.carcv.core.model.file.FileEntry;

/**
 * @author oskopek
 *
 */
public class CarSorter {
    
    /**
     * Returns a list of fileEntries from given images in a fileentry directory
     * --> this means you should probably remove this fileentry from the list after merging this List in.
     * @param directory
     * @return
     */
    public static List<FileEntry> sortIntoCars(FileEntry directory) {//TODO 1 Implement: sort them into multiple FileEntries
        
        return null;
    } 

}
